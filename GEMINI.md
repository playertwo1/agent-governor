# Antigravity execution rules

Follow `AGENTS.md`. Before implementation, define `.governor/task-contract.json`. Do not edit `.governor/`, `.agents/hooks/`, or the Governor CI workflow during an ordinary feature task. A denied action requires human review; do not attempt an equivalent bypass. Completion requires the commands and evidence named in the active task contract.

